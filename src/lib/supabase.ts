import { createClient } from '@supabase/supabase-js'; const u=process.env.SUPABASE_URL!; const k=process.env.SUPABASE_SERVICE_ROLE_KEY!; export const supabase = createClient(u,k,{auth:{persistSession:false}});
