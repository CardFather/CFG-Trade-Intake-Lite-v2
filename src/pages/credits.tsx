import { useState } from 'react'; export default function C(){ const [cid,setCid]=useState(''); const [amt,setAmt]=useState(''); const go=async()=>{const r=await fetch('/api/credits/redeem',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({shopify_customer_id:cid,amount_cents:Math.round((parseFloat(amt)||0)*100),reason:'POS redemption'})}).then(r=>r.json()); if(r.error) alert(r.error); else alert('New balance (cents): '+r.balance_cents)}; return (<div style={{padding:20}}><h1>Redeem Credit</h1><input placeholder='Shopify customer ID' value={cid} onChange={e=>setCid(e.target.value)} /> <input placeholder='Amount $' value={amt} onChange={e=>setAmt(e.target.value)} /> <button onClick={go}>Redeem</button></div>); }
